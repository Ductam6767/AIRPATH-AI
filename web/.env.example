# Optional. Unset locally so Vite proxies /demo and /health to localhost:8000.
# For browser dev without local FastAPI, copy to `.env.development`:
# VITE_API_URL=https://airpath-ai-dataporsche-u8gd.onrender.com
# Set at Render frontend build time to the public FastAPI URL (no trailing slash).
# VITE_API_URL=https://your-airpath-api.onrender.com
