# AIRPATH-AI web UI placeholder

The React frontend is intentionally not implemented in WEB-1.

This directory is reserved for the upcoming map-first prototype that will
consume the thin FastAPI demo API (`/demo/scenarios`, `/demo/routes`).

Do not place research-engine logic here.
